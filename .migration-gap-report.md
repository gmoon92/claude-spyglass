# P1-01 Migration Gap Report

> worktree: `claude-spyglass-rm` · branch `feat/react-migration` · baseline `2ae3c39`
> 작성 시점: 2026-05-31 (P1-01 Vite+React18+TS 빌드 파이프라인 스캐폴딩)

P1-01 작업 중 발견된, 임의 추측으로 처리하지 않고 휴먼/후속 task 판단이 필요한 항목.

---

## GAP-1 (BLOCKER 아님, 게이트 부재): `check-architecture.sh` 가 worktree 에 존재하지 않음

- **tasks.json P1-01 test_strategy/done_criteria**: "빈 packages/web/src/ 생성 직후 check-architecture.sh 1회 실행해 KEBAB 게이트 가동 확인", "빈 src/ 에서 check-architecture.sh 통과 확인".
- **실측**: baseline `2ae3c39` 전체 레포에 `check-architecture.sh`(및 유사 architecture 스크립트)가 **존재하지 않음**. `scripts/`, `.githooks/`(디렉토리 자체 부재), repo 전역 `find`/`grep KEBAB` 모두 0건.
- **영향**: tasks.json 이 가정한 "web 첫 src 커밋 = KEBAB 게이트 첫 실전"을 자동 스크립트로 가동할 수 없음. tasks.json constraints 의 `static_gate` 항목도 동일 스크립트 전제.
- **임시 처리(이번 task)**: KEBAB 불변식을 **수동 검증**으로 대체.
  - `packages/web/src/` 하위 디렉토리: `src` 단일(케밥 준수).
  - 파일: `main.tsx`(엔트리), `vite-env.d.ts`(env 선언) — 컴포넌트 아님, 컨벤션상 lowercase 허용.
  - 위반 0.
- **필요 판단(휴먼)**: `check-architecture.sh` 가
  (a) 메인 레포(`claude-spyglass`)에만 있고 worktree 에 미반영인지,
  (b) 아직 작성되지 않았는지(P1-08 게이트 보강 예정인지)
  확인 필요. tasks.json constraints 는 "현 check-architecture.sh 는 DEP 규칙을 정적 강제 못함"이라고 *현존*을 전제하므로, 스크립트 부재는 명세-실태 불일치다. P1-08 또는 별도 게이트 보강 task 에서 스크립트 실제 도입/연결 권장.

---

## GAP-2 (정보, 회귀 아님): 로컬 전역 `tsc` 6.0.3 vs lockfile-pinned 5.9.3

- **현상**: worktree 에 `node_modules` 미설치 상태에서 `bunx tsc` 가 전역 캐시의 **TypeScript 6.0.3** 를 해석 → `tsconfig.json:15 baseUrl` 가 TS6.0 에서 **하드 에러(TS5101)** 로 떨어져 typecheck exit 2. tasks.json 의 "baseline typecheck exit 0" 와 외관상 불일치.
- **근본 원인**: 본 패키지/루트는 `typescript: ^5.0.0`(루트 `package.json`), `bun.lock` 은 `typescript@5.9.3` 핀. CI `setup-bun` + `bun install --frozen-lockfile` 경로는 5.9.3 사용 → `baseUrl` 경고가 에러 아님.
- **처리**: `bun install --frozen-lockfile` 로 5.9.3 설치 후 baseline typecheck **exit 0 재확인**. 이후 모든 게이트는 `node_modules/.bin/tsc`(=5.9.3, `bun run --cwd packages/web typecheck`) 로 실행.
- **권장**: 로컬 개발 시 worktree 에서 `bun install` 선행 필수. 전역 tsc 로 typecheck 하지 말 것(버전 드리프트). TS7.0 대비 `baseUrl` deprecation(`ignoreDeprecations`/paths 상대화)은 별도 트랙으로 후속 검토(P5-02 strict 승격 시 동반 가능).

---

## 보류(이번 task 범위 밖, 명시적 후속 귀속)

- **locales → dist 복사**: done_criteria 의 `vite build(dist/assets) 산출` 은 충족(React 전용 엔트리 빌드). 단 운영 데몬이 `dist` 를 서빙할 때 `/locales/*` 정합(publicDir/viteStaticCopy)은 **P1-02 서빙 계약 결정** 소관 → 본 task 미처리(주석으로 vite.config.ts 에 명시). dev 모드는 `/locales` 프록시(→9999)로 동작 확인 완료.
- **dispatch.ts mimeMap 확장 / WEB_ROOT→dist / SPA fallback**: 백엔드(`packages/server`) 변경이라 P1-01 무수정 원칙상 미처리. P1-02(문서)·P4-07/P4-08(실제 변경) 소관.
- **운영 진입(index.html) 전환**: vanilla `index.html` 무수정 병존. React 진입 단일화는 P4-07.
