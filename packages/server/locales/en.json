{
  "checks": {
    "integrity": {
      "orphan": {
        "ok": "orphan rows (turn_id NULL): 0",
        "warn": "orphan rows {count} — tool_call/response not bound to any turn",
        "hint": "session resume or hook missing. UI exposes as \"session prologue\" section (ADR-001 P1)",
        "unavailable": "cannot check orphan rows (DB missing or error)"
      },
      "zeroResponseTurns": {
        "ok": "turns with 0 responses: 0",
        "warn": "turns with 0 responses: {count}",
        "hint": "tool-only turn (normal) or Stop/proxy fallback all missing (abnormal). manual check required",
        "unavailable": "cannot check turns with 0 responses"
      },
      "longProxyResponses": {
        "ok": "proxy responses over 120s: 0",
        "warn": "proxy responses over 120s: {count}",
        "hint": "v23(P1-E) exact-match path unaffected. residual risk only in fallback time-based cross-link path — informational",
        "unavailable": "cannot check proxy responses over 120s"
      },
      "duplicateResponses": {
        "ok": "duplicate responses: 0",
        "fail": "duplicate responses {count} pairs — same message saved as two rows",
        "hint": "should be 0 after ADR-001 P1-A fix. possible code regression — check change history",
        "unavailable": "cannot check duplicate responses"
      },
      "mismatchedTurnIds": {
        "ok": "mismatched turn_id: 0",
        "fail": "mismatched turn_id {count} — rows tagged incorrectly by timestamp",
        "hint": "should be 0 after applying ADR-001 P1-A getTurnIdAt. regression possible",
        "unavailable": "cannot check mismatched turn_id"
      },
      "unlinkedToolCalls": {
        "ok": "tool_call api_request_id match 100% ({total} rows)",
        "okSample": "tool_call {total} rows (insufficient sample, match rate evaluation skipped)",
        "okPartial": "tool_call api_request_id unmatched {unlinked}/{total} ({pct}%)",
        "warn": "tool_call api_request_id unmatched {unlinked}/{total} ({pct}%)",
        "hint": "suspect proxy SSE tool_use capture miss or hook arrival order reversal (ADR-001 P1-E)",
        "unavailable": "cannot check tool_call api_request_id match",
        "noRecent": "no tool_call in the last hour (check skipped)"
      },
      "orphanProxyToolUses": {
        "ok": "proxy_tool_uses orphan: 0",
        "okWithCount": "proxy_tool_uses orphan {count} rows (usually user-cancelled tool_use)",
        "unavailable": "cannot check proxy_tool_uses orphan"
      }
    },
    "environment": {
      "bun": {
        "ok": "Bun {version}",
        "fail": "Bun {version} (require ≥ 1.0)",
        "failNotInstalled": "Bun is not installed",
        "hintUpgrade": "run bun upgrade",
        "hintInstall": "install from https://bun.sh/install"
      },
      "settingsJson": {
        "ok": "settings.json OK",
        "failMissing": "settings.json is missing",
        "failParse": "settings.json JSON parse failed",
        "hintInstall": "run: curl -fsSL https://raw.githubusercontent.com/gmoon92/claude-spyglass/main/scripts/install.sh | bash",
        "hintFixJson": "fix to valid JSON format"
      },
      "hooksRegistered": {
        "ok": "hooks registered (SPYGLASS_DIR: {dir})",
        "warnNoDir": "hooks configured, but SPYGLASS_DIR not set",
        "failNotRegistered": "hooks are not registered",
        "failUnavailable": "cannot check hooks (settings.json missing)",
        "hintSetDir": "set env.SPYGLASS_DIR (e.g. ~/.spyglass-src or /path/to/claude-spyglass)",
        "hintAddHooks": "add spyglass-collect.sh hooks to settings.json",
        "hintNeedSettings": "settings.json is required"
      },
      "hookExecutable": {
        "ok": "hook script executable permission OK",
        "warnNoDir": "cannot check hook script (SPYGLASS_DIR not set)",
        "failMissing": "hook script missing: {path}",
        "failNoPermission": "hook script not executable: {path}",
        "failUnavailable": "cannot locate hook script",
        "failCheckFailed": "hook script check failed",
        "hintSetDir": "set env.SPYGLASS_DIR in settings.json",
        "hintCheckPath": "check SPYGLASS_DIR path",
        "hintChmod": "chmod +x {path}",
        "hintNeedSettings": "settings.json is required",
        "hintCheckFormat": "check settings.json format"
      }
    },
    "database": {
      "permissions": {
        "ok": "DB permission: {perms}",
        "warn": "DB permission: {perms} (recommended: 600)",
        "warnNoDb": "DB file does not exist yet",
        "fail": "DB file permission check failed",
        "hintChmod": "chmod 600 {path}",
        "hintAutoCreate": "will be created automatically when server starts",
        "hintCorrupted": "DB file may be corrupted"
      },
      "schemaVersion": {
        "ok": "DB schema v{version}",
        "warnUnknown": "DB schema version unknown (v0)",
        "warnOld": "DB schema v{version} (recommended: v12+)",
        "warnNoDb": "cannot check schema version (DB file missing)",
        "warnCheckFailed": "DB schema version check failed",
        "hintMigrate": "Track 1 migration may be incomplete, or reset the DB",
        "hintAutoCreate": "will be created automatically when server starts",
        "hintCorrupted": "DB file may be corrupted"
      },
      "recentActivity": {
        "ok": "collection activity within last 5 minutes",
        "warn": "last collection: {minutes} minutes ago",
        "warnNoData": "no collected data",
        "warnUnavailable": "cannot check recent activity (DB missing)",
        "warnError": "error while checking collection activity",
        "hintRun": "run Claude Code and execute bun run dev",
        "hintAutoCreate": "start the server and run a command in Claude Code",
        "hintCorrupted": "DB may be corrupted"
      }
    },
    "server": {
      "port": {
        "ok": "port {port} available",
        "warn": "port {port} in use",
        "hint": "another process is using the port. check with lsof -i :{port}"
      }
    }
  },
  "fix": {
    "dbPermissionOk": "DB permission fixed: chmod 600 {path}",
    "dbPermissionFail": "DB permission fix failed: {path}",
    "hookPermissionOk": "hook script permission fixed: chmod +x {path}",
    "hookPermissionFail": "hook script permission fix failed",
    "dupRemoved": "duplicate responses removed: {count} rows (ADR-001 P1-A)",
    "mismatchFixed": "mismatched turn_id fixed: {count} rows (ADR-001 P1-A)",
    "orphanMapped": "orphan turn_id retroactively mapped: {count} rows (ADR-001 P1-B)",
    "dataFixFail": "data consistency fix failed: {error}"
  },
  "cli": {
    "usage": "Usage: bun run packages/server/src/cli.ts doctor [--fix]",
    "error-prefix": "Error:"
  },
  "doctor": {
    "title": "🔍 spyglass environment check",
    "auto-fix-done": "Auto-fix complete. Run doctor again",
    "fail-summary": "{fail} failed, {warn} warned",
    "fail-hint": "Follow the hints above to resolve issues",
    "warn-summary": "{warn} warned",
    "warn-hint": "See the hints above if needed",
    "all-pass": "All checks passed!"
  }
}
